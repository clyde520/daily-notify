# 🚀 每日信息推送系统 - 完整设置指南

## 第一步：创建 GitHub 仓库

1. 打开浏览器访问：https://github.com/new

2. 填写仓库信息：
   - **Repository name**（仓库名）：输入 `daily-notify`
   - **Description**（描述）：可以不填，或者填 `每日信息自动推送系统`
   - 选择 **Public**（公开仓库）
   - ⚠️ **不要勾选**任何选项框（不要勾选 Add a README file 等）

3. 点击绿色的 **"Create repository"** 按钮

---

## 第二步：上传文件

### 准备工作
找到这个文件夹：
```
c:\Users\Administrator\WorkBuddy\20260312134537
```

### 上传所有文件
1. 在新建的仓库页面，点击链接：**"uploading an existing file"**

2. 一次性选中以下**7个文件**，拖入网页上传区域：

| 文件名 | 位置 |
|--------|------|
| `fetch_and_notify.py` | 根目录 |
| `requirements.txt` | 根目录 |
| `config.json` | 根目录 |
| `.env.example` | 根目录 |
| `README.md` | 根目录 |
| `上传指引.md` | 根目录 |
| `完整设置指南.md` | 根目录 |

3. 上传完成后，滚动到页面底部

4. 在 **"Commit changes"** 输入框输入：`Initial commit`

5. 点击绿色按钮 **"Commit changes"`

---

## 第三步：创建工作流文件（重要！）

### 1. 创建 workflows 文件夹和配置文件
1. 在仓库页面，点击绿色按钮 **"Add file"** → **"Create new file"**

2. 在文件名输入框中输入：
   ```
   .github/workflows/daily-report.yml
   ```
   ⚠️ **注意**：必须包含 `.github/workflows/` 这部分

3. 在编辑框中复制粘贴以下内容：

```yaml
name: 每日信息推送

on:
  # 定时触发：每天早上9点（北京时间）
  schedule:
    - cron: '0 1 * * *'  # UTC时间1点 = 北京时间9点

  # 手动触发（用于测试）
  workflow_dispatch:

jobs:
  fetch-and-notify:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 运行信息搜集脚本
        env:
          # 通知配置
          NOTIFICATION_TYPE: ${{ secrets.NOTIFICATION_TYPE }}
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}

          # 信息搜集配置
          WEATHER_CITY: ${{ secrets.WEATHER_CITY }}
          NEWS_KEYWORD: ${{ secrets.NEWS_KEYWORD }}
          STOCK_SYMBOL: ${{ secrets.STOCK_SYMBOL }}

        run: |
          python fetch_and_notify.py
```

4. 滚动到页面底部
5. 在 "Commit new file" 输入框输入：`Add workflow file`
6. 点击绿色按钮 **"Commit changes"**

---

## 第四步：配置 Secrets（飞书 Webhook）

### 1. 进入 Secrets 设置页面
1. 在仓库页面顶部，点击 **"Settings"** 标签
2. 左侧菜单中，找到并点击 **"Secrets and variables"**
3. 展开后，点击 **"Actions"**
4. 点击 **"New repository secret"** 按钮

### 2. 添加 Secrets（需要添加2个）

#### Secret 1：通知方式
| Name（名称） | Value（值） |
|--------------|-------------|
| `NOTIFICATION_TYPE` | `feishu` |

点击 **"Add secret"** 按钮

#### Secret 2：飞书 Webhook
| Name（名称） | Value（值） |
|--------------|-------------|
| `FEISHU_WEBHOOK` | `https://open.feishu.cn/open-apis/bot/v2/hook/2602bdf8-db15-47a7-9a38-9ac223355317` |

点击 **"Add secret"** 按钮

---

## 第五步：测试运行

### 1. 进入 Actions 页面
1. 点击仓库顶部的 **"Actions"** 标签

### 2. 手动触发工作流
1. 左侧应该显示 **"每日信息推送"** 工作流
2. 点击进入该工作流
3. 点击右侧的 **"Run workflow"** 按钮
4. 在弹出的窗口中点击绿色的 **"Run workflow"** 按钮

### 3. 查看运行结果
1. 等待 1-2 分钟
2. 刷新页面，会显示运行记录
3. 如果显示绿色的 ✅，表示成功
4. 如果显示红色的 ❌，点击查看错误日志

### 4. 检查飞书消息
成功后，您的飞书会在几分钟内收到一条包含"报告"关键词的消息

---

## 第六步：验证定时任务

- 定时任务会在每天 **北京时间早上 9:00** 自动运行
- 您可以在 Actions 页面查看历史运行记录
- 每天早上 9:00 收到自动推送的消息

---

## ✅ 完成检查清单

- [ ] 1. 创建了名为 `daily-notify` 的 GitHub 仓库
- [ ] 2. 上传了所有7个文件
- [ ] 3. 创建了 `.github/workflows/daily-report.yml` 文件
- [ ] 4. 添加了 `NOTIFICATION_TYPE` Secret（值为 feishu）
- [ ] 5. 添加了 `FEISHU_WEBHOOK` Secret
- [ ] 6. 手动触发测试成功
- [ ] 7. 飞书收到了测试消息

---

## ❓ 常见问题

### Q1: 上传文件时提示文件已存在怎么办？
A: 可以点击 "Replace" 替换，或者删除原有文件后重新上传

### Q2: Actions 页面没有显示工作流？
A: 检查 `.github/workflows/daily-report.yml` 文件是否正确创建，文件名必须包含 `.github/workflows/` 路径

### Q3: 收不到飞书消息？
A: 检查：
- Secrets 是否正确配置
- 飞书机器人的安全设置是否设置了关键词"报告"
- 查看 Actions 运行日志中的错误信息

### Q4: 想修改推送时间？
A: 编辑 `.github/workflows/daily-report.yml` 文件，修改 cron 表达式

---

## 获取帮助

如果遇到问题：
1. 查看 Actions 页面的运行日志，查找错误原因
2. 检查 Secrets 配置是否正确
3. 确认飞书 Webhook 地址是否有效

---

## 您的飞书 Webhook 地址
```
https://open.feishu.cn/open-apis/bot/v2/hook/2602bdf8-db15-47a7-9a38-9ac223355317
```

请妥善保管，不要泄露给他人！
