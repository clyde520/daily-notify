# 📋 上传文件到 GitHub 步骤指引

## 前置信息
- GitHub 用户名：clyde520
- 仓库名称：信息推送
- 飞书 Webhook：https://open.feishu.cn/open-apis/bot/v2/hook/2602bdf8-db15-47a7-9a38-9ac223355317

---

## 第一步：创建 GitHub 仓库

1. 打开浏览器访问：https://github.com/new
2. 填写仓库信息：
   - **Repository name**：`信息推送`
   - **Description**（可选）：`每日信息自动推送系统`
   - 选择 **Public**（公开仓库，免费额度更多）
   - 不要勾选任何选项框
3. 点击绿色的 **"Create repository"** 按钮

---

## 第二步：上传文件

### 2.1 点击上传
在新建的仓库页面，点击链接：
```
uploading an existing file
```

### 2.2 准备文件
打开文件夹：
```
c:\Users\Administrator\WorkBuddy\20260312134537
```

将以下**6个文件**全部拖入网页上传区域：

| 文件名 | 所在位置 |
|--------|----------|
| `fetch_and_notify.py` | 根目录 |
| `requirements.txt` | 根目录 |
| `config.json` | 根目录 |
| `.env.example` | 根目录 |
| `README.md` | 根目录 |
| `daily-report.yml` | `.github/workflows/` 文件夹内 |

**注意**：
- 可以一次性全选这6个文件然后拖入
- 确保文件夹结构正确，特别是 `.github/workflows/` 里的文件

### 2.3 提交文件
1. 滚动到页面底部
2. 在 **"Commit changes"** 输入框输入：`Initial commit`
3. 点击绿色按钮 **"Commit changes"**

---

## 第三步：配置 Secrets（飞书 Webhook）

### 3.1 进入 Secrets 设置
1. 在仓库页面顶部，点击 **"Settings"** 标签
2. 左侧菜单找到并点击 **"Secrets and variables"**
3. 点击展开，选择 **"Actions"**
4. 点击 **"New repository secret"** 按钮

### 3.2 添加 Secrets

需要添加以下 **2个** Secrets：

#### Secret 1: 通知方式
| Name | Value |
|------|-------|
| `NOTIFICATION_TYPE` | `feishu` |

#### Secret 2: 飞书 Webhook
| Name | Value |
|------|-------|
| `FEISHU_WEBHOOK` | `https://open.feishu.cn/open-apis/bot/v2/hook/2602bdf8-db15-47a7-9a38-9ac223355317` |

---

## 第四步：测试运行

### 4.1 手动触发
1. 点击仓库顶部的 **"Actions"** 标签
2. 找到 **"每日信息推送"** 工作流
3. 点击右侧的 **"Run workflow"** 按钮
4. 点击绿色的 **"Run workflow"** 确认

### 4.2 查看结果
- 等待 1-2 分钟
- 如果成功，您的飞书会收到一条消息（包含"报告"关键词）
- 如果失败，点击运行记录查看错误日志

---

## 第五步：验证定时任务

定时任务会在每天 **北京时间早上 9:00** 自动运行。

首次运行后，可以：
1. 在 **"Actions"** 页面查看历史运行记录
2. 每天早上 9:00 收到自动推送的消息

---

## ✅ 完成检查清单

- [ ] 1. 创建了 GitHub 仓库
- [ ] 2. 上传了 6 个文件
- [ ] 3. 添加了 `NOTIFICATION_TYPE` Secret（值为 feishu）
- [ ] 4. 添加了 `FEISHU_WEBHOOK` Secret
- [ ] 5. 手动触发测试成功
- [ ] 6. 飞书收到了测试消息

全部勾选完成，系统就配置好了！

---

## ❓ 常见问题

**Q1: 上传文件时提示错误怎么办？**
A: 确保一次性拖入所有文件，不要分多次上传。

**Q2: Actions 没有显示工作流？**
A: 刷新页面，确保 `.github/workflows/daily-report.yml` 文件已正确上传。

**Q3: 收不到飞书消息？**
A: 检查 Secrets 是否正确配置，关键词是否设置为"报告"。

**Q4: 想修改通知时间？**
A: 编辑 `.github/workflows/daily-report.yml` 文件，修改 cron 表达式。

---

需要帮助？可以查看 README.md 获取更多详细说明！
